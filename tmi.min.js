
// SIMULATED TMI.JS (stubbed for demo)
// In actual deployment this would be the full tmi.min.js library
window.tmi = {
  Client: function (config) {
    return {
      connect: () => {
        console.log("Connected to Twitch chat (simulated)");
        setTimeout(() => {
          const event = new Event("tmi-ready");
          window.dispatchEvent(event);
        }, 1000);
      },
      on: (event, handler) => {
        if (event === 'message') {
          window.addEventListener("tmi-ready", () => {
            setInterval(() => {
              handler('#windiestink562', { 'display-name': 'Viewer420' }, 'Oi Sheila! Light another one!', false);
            }, 5000);
          });
        }
      }
    };
  }
};
